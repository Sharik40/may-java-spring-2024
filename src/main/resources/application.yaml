spring:
  application:
    name: may-java-spring-2024
  profiles:
    active: store1
  datasource:
    url: "jdbc:postgresql://localhost:5432/springhw"
    username: "postgres"
    password: "admin"
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  #logging:
  #  level:
  #    root: DEBUG
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
